# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key

# LLM Provider Configuration
# Supported providers: ollama | openai
# Default: ollama (for local development)
LLM_PROVIDER=ollama

# Ollama Configuration (for local development with LLM_PROVIDER=ollama)
OLLAMA_HOST=http://localhost:11434
OLLAMA_VALIDATION_MODEL=llama3.1
OLLAMA_GENERATION_MODEL=llama3.1

# OpenAI Configuration (for production with LLM_PROVIDER=openai)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4-turbo-preview
